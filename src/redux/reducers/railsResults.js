import { type as findRailsResultsType } from '../actions/findRailsResults';

const defaultState = [];

function reducer(state = defaultState, {type,payload}) {
    switch (type) {
        default:
            return state;
    }
}

export default reducer;